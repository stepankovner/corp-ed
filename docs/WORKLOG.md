# Журнал работ бэкенда

Что сделано, чем проверено, где ошибались и что дальше. Обновляется в
конце каждого этапа. Решения с альтернативами — `DECISIONS.md`, карта
системы — `ARCHITECTURE.md`, незакрытое — `RISKS.md`.

---

## Сводка на 26 сентября 2026 (после живой проверки Битрикс24; этапы 2–4а коннекторов — Битрикс24, Confluence, Яндекс Диск)

| | |
|---|---|
| Ветка | `claude/gallant-pascal-xtb35t` → PR [#15](https://github.com/stepankovner/corp-ed/pull/15) в `main` (не слит) |
| Тесты | **1167 passed**, 0 skipped при `TEST_REDIS_URL`; ~2 мин с Postgres и Redis; плюс 1 живой тест в `tests/live/` (пропускается без переменных тестового портала) |
| Покрытие | **92 %** (ветвевое), порог в CI 85 % |
| Проверки | ruff (E, W, F, I, N, UP, B, SIM, **S**) по всему репозиторию, ruff format, mypy strict на `src` — чисто |
| CI на HEAD | [CI #142](https://github.com/stepankovner/corp-ed/actions/runs/36185087126) ✅ (линт, mypy, 1160 тестов, миграции на пустой базе, образ без root по digest), [Security #36](https://github.com/stepankovner/corp-ed/actions/runs/36185087138) ✅ (`1c5789d`); все промежуточные пуши этой ночи тоже зелёные |
| Миграции | 14 ревизий, без изменений на этапе 2 (схема этапа 1 покрыла OAuth: `external_user_id`, шифрованные `credentials` у грантов и подключений) |
| Слияния ML | 5 (`ml/alice-compare` ×2, `ml/docs` ×2, `ml/multi-query` — с #16 M2, #17 золотой dev, #18 multi-query), файлы ML не редактировались |
| Живая проверка этапов 2–4а | **Битрикс24 — выполнена по вебхуку 26.09** (сеть открыта): `connector-check` против тестового портала, записи ответов в `fixtures/bitrix24/live/`, синхронизация личного диска через `ConnectorSyncService` с настоящей песочницей (`tests/live/`); найдены и исправлены ошибки №31–33. **Не проверены**: OAuth сотрудника (нужен вход в портал), база знаний (на портале её нет), scope `note`, ответ со ссылкой (нет ключей Yandex Cloud). Confluence и Яндекс Диск — по-прежнему нет тестовых систем (см. «Этап 3», «Этап 4а») |

Состав тестов по каталогам:

| Каталог | Тестов | Что |
|---|---|---|
| `tests/security/` | 177 | токены, пароли, периметр, лимиты, аудит, RLS, шифрование секретов, SSRF, **state OAuth, редирект без следования, режим `via_proxy`** |
| `tests/api/` | 206 | все группы ручек: роли, изоляция, валидация, коды; коннекторы, **OAuth Битрикс24** |
| `tests/llm/`, `tests/ingest/` | 57 + 36 | адаптеры, разбор ответов, файлы, песочница (**бюджет CPU, код при убийстве по лимиту**) |
| `tests/connectors/` | 134 | **Битрикс24: клиент (старый REST и 3.0), адаптер (диск, база знаний, база знаний 2.0, OAuth), контракт по фикстурам документации; Confluence: клиент, обход с правами, storage-формат; Яндекс Диск: OAuth, обход, продление** |
| `tests/test_*.py` (сервисы бэкенда) | 283 | FAQ, гибрид, кредиты, компании, ингест, воркер, пробелы, изоляция, синхронизация коннекторов, видимость, очередь синхронизации, HTML, **синхронизация с адаптерами Битрикс24, Confluence и Яндекс Диска, `cli connector-check`** |
| `tests/live/` | 1 | **живая синхронизация с тестовым порталом Битрикс24** — только с `BITRIX24_TEST_PORTAL`/`WEBHOOK`, в CI пропускается |
| ML (`tests/ml_eval/` 131 + чистые функции ML в `tests/test_*.py` 143; после слияния #16–#18) | 274 | не редактируются бэкендом |
| **Итого** | **1160** | `pytest --collect-only`, 25.09; +187 к этапу 1 |

---

## Как работает код — короткая карта

Подробно с потоками — `ARCHITECTURE.md`, раздел 5. Здесь — где что искать.

- **Вопрос сотрудника** — `services/faq_service.py::answer`. Порядок:
  пул кредитов → режим компании → словарь (только в поиск) → эмбеддинг →
  поиск (`vector` или `hybrid`, `_retrieve`) → бюджет контекста → модель
  → пометка/отказ → строка `qa_log` и пороги кредитов одной транзакцией.
- **Документ** — `services/material_service.py::upload` (проверки в
  процессе API, разбор в дочернем `ingest/sandbox.py`), очередь
  `ingest_jobs`, воркер `worker.py` → `services/ingest_service.py`.
- **Изоляция** — `core/database.py` (хуки), `core/tenant_context.py`,
  `core/db_policies.py` (RLS и триггер аудита; один источник для
  миграций и тестовой схемы).
- **Вход и сессии** — `services/auth_service.py`, `core/security.py`,
  `api/v1/dependencies.py::get_current_user_allow_password_change`.
- **Ночные задачи** — `cli.py`: `purge` (`services/retention_service.py`),
  `gaps` (`services/gap_report_service.py`).
- **Настройки** — `core/config.py`: `Settings`, `LLMSettings`,
  `RagSettings` (без дефолтов — значения ML), `GapsSettings`,
  `BillingSettings`, `HttpSettings`.
- **Где ловится ошибка** — `core/exception_handlers.py` +
  `core/middleware.py::RequestIDMiddleware` (необработанное → JSON 500).
- **Коннекторы** — настройка: `services/connector_service.py` и
  `api/v1/endpoints/connectors.py` (в том числе OAuth: `oauth_start`,
  `oauth_callback`); синхронизация:
  `services/connector_sync_service.py` (в воркере, `worker.py::SyncWorker`
  + планировщик раз в минуту); адаптеры и каталог: `connectors/`
  (`registry.py` — виды, `default_registry(settings)`, `AdapterOptions`,
  `config_check`/`credentials_check`; `common.py` — `TokenSet`, `redact`,
  помощники; `bitrix24/` — `client.py`, `oauth.py`, `disk.py`,
  `knowledge_base.py`, `notes.py`, `adapter.py`; `confluence/` —
  `client.py`, `storage.py`, `adapter.py`; `yandex/` — `oauth.py`,
  `disk.py`, `adapter.py`); проверка против источника без базы:
  `services/connector_check_service.py` + `cli connector-check`;
  секреты: `core/secrets.py`; state OAuth: `core/security.py`; защита
  от SSRF: `core/outbound.py`; права на документ: `materials.visibility`
  + `material_access`, фильтр `_visible_to` в
  `repositories/chunk_repository.py`.

---

## Что сделано (по этапам)

| Этап | Содержание | Ключевые коммиты |
|---|---|---|
| 1. ML-стек | слияние веток ML, `ChunkMatch` с крошками, `embed_text`, промпт v2, бюджет контекста | `7f2aa4f` |
| 2. Пивот | удалены программы/брифы/треки, роли `ADMIN`/`EMPLOYEE` | миграция `577fd323ce34` |
| 3. Аутентификация | access 15 мин + refresh с ротацией, `token_version`, argon2, политика паролей, пользователи, CLI компаний | миграция `045621dbe193` |
| 4. Периметр и изоляция | заголовки, лимиты тела, 422 без эха, лимиты частоты (Redis), аудит с триггером, **RLS** | `1bc0bfec7e55`, `73493006a1b1` |
| 5. Файлы и ингест | docx/pdf/txt/md, песочница, очередь и воркер, переиндексация, адаптер Flash, v2@768 | `082d6d6c3590`, `97d70ebf2e07`, `d7e7594826a0` |
| 6. Продукт | `qa_log` и оценки, кредиты (402), Р1 + строгий режим, гибрид под флагом, словарь, отчёт о пробелах, BH-25/26 | `3783ee8` … `63b343c` |
| 7. CI и поставка | security-workflow, образ без root, compose в боевой форме, документация | `4945f87`, `b53ffe5`, `8184f2a` |
| 8. Коннекторы, этап 0 | дизайн, исследование рынка и API, решения команды | `1cbc401`, `7f93e5a`, `599c41a` |
| 9. Коннекторы, этап 1 | ядро: 5 таблиц под RLS, поля источника у материалов, шифрование секретов, SSRF-защита, два режима прав, синхронизация, воркер, `/connectors`, фильтр видимости в поиске | миграция `42a0ae63f5ff` |
| 10. Коннекторы, этап 2 | Битрикс24: REST-клиент, OAuth (старт, обратный вызов, продление), модули диска и базы знаний, секрет приложения у подключения, `AdapterConfigError`, `cli connector-check` с записью фикстур; модуль «База знаний 2.0» (REST 3.0, Markdown); одноразовый state, потоковое скачивание, закрепление actions и образа | `cd99dd6`, `adb01be`, `64f3fb2`, `ee726c9` |
| 11. Коннекторы, этап 3 | Confluence Server/DC: режим organization, страницы и вложения, права из ограничений по цепочке предков с раскрытием групп, storage-формат → HTML; **без документации и живого сервера** | `8ade4b1` |
| 12. Коннекторы, этап 4а | Яндекс 360: OAuth Яндекс ID (тот же механизм per_user), модуль Диска сотрудника с общими папками, скачивание по подписанной ссылке; **без документации и живого Диска**; Вики отложена | этот коммит |

Сверка с контрактом ML — `INTEGRATION.md`; с досье — раздел
«Дальнейшие действия» ниже.

---

## Ошибки и исправления

Что пошло не так по ходу работы, почему, и что сделано. Порядок —
хронологический.

| # | Ошибка | Причина | Исправление | Альтернативы, которые отвергли |
|---|---|---|---|---|
| 1 | `UndefinedColumnError` на старой тестовой базе | `create_all` не меняет существующие таблицы | схема пересоздаётся на каждый прогон (`DROP SCHEMA … CASCADE`) | миграции в тестах — медленнее и дублирует job CI |
| 2 | Токен с чужим `tenant_id` доставал пользователя | `session.get` берёт объект из identity map мимо фильтра тенанта | `get_by_id` через `select`; явная сверка `user.tenant_id` | полагаться только на хук — одно кольцо вместо трёх |
| 3 | 413 превращался в 400 | FastAPI оборачивает исключение из `receive` | middleware сам отвечает 413 и закрывает поток | лимит только на прокси — приложение осталось бы без защиты |
| 4 | Миграция под FORCE RLS «не видела» строк | владелец-несуперпользователь тоже под политикой | `TRUNCATE` / `NO FORCE … FORCE` в миграции; правило в `db_policies.py`; job CI под таким владельцем | гонять миграции суперпользователем — прячет именно этот класс ошибок |
| 5 | `sa.Enum` не создавал тип при `add_column` | известная ловушка alembic | `postgresql.ENUM(...).create(checkfirst=True)` | — |
| 6 | Тесты RLS падали только после тестов отчёта о пробелах | `SET ROLE` в первой транзакции нового соединения откатывался вместе с ней; соединение возвращалось суперпользователем | `commit()` сразу после `SET ROLE` в фикстуре; регрессионный тест на 4 свежих соединения | отдельная роль с `LOGIN` — меняет строку подключения тестов |
| 7 | `pip-audit` при первом прогоне: 7 CVE (`anyio`, `pydantic-settings`, `starlette`) | зависимости не проверялись | точечный `uv lock --upgrade-package`, тесты без изменений | — |
| 8 | `aquasecurity/trivy-action@0.28.0` не разрешился | тег не найден в этой среде | запуск официального образа `ghcr.io/aquasecurity/trivy` напрямую | `@master` — незакреплённая ссылка в security-конвейере |
| 9 | `cli --help` падал в образе | `configure_logging()` читал настройки до разбора аргументов | сначала аргументы; тест в подпроцессе с пустым окружением | передать переменные в smoke-тест — прятало бы дефект |
| 10 | Коммит `63b343c` ушёл в CI с падающим тестом | `pytest \| tail` скрыл код возврата, цепочка `&&` дошла до `push` | исправление через 90 с (`0ee1f5f`); дальше `set -o pipefail` | — |
| 11 | Ложные срабатывания bandit (`S105` на именах событий, `S311` на джиттере) | правила по именам | `noqa` с причиной на каждой строке | отключить правила целиком — потеряли бы настоящие находки |
| 12 | `MissingGreenlet` в синхронизации после сбоя одного документа | `session.rollback()` сбрасывает атрибуты ORM-объектов; следующее чтение `connector.id` шло в базу из синхронного кода | сервис держит плоские копии полей (`_Run.connector_id`, `kind`, `modules`, `config`), гранты — кортежами; ORM-объекты читаются только после `session.refresh` | `expire_on_rollback=False` — скрывает устаревшие данные во всех сервисах |
| 13 | Покрытие `connector_service.py` 50 % при полностью зелёных тестах | coverage без `concurrency = greenlet` теряет трассировку после первого `await` к базе (SQLAlchemy async работает в greenlet) | `concurrency = ["thread", "greenlet"]` в `pyproject.toml`; общее покрытие оказалось 92 %, а не 87 % | поднять порог без починки измерения — цифра была бы случайной |
| 14 | Автогенерация миграции пыталась создать уже существующий тип `ingestjobstatus` и пропустила смену частичного индекса sha256 | alembic не различает переиспользование enum и не сравнивает `WHERE` частичных индексов | `postgresql.ENUM(..., create_type=False)`, индекс пересоздан явно, RLS и CHECK добавлены руками; прогон `upgrade → check → downgrade → upgrade` | — |
| 15 | **Живая проверка**: `POST /connectors/{id}/test` для вида без адаптера → 500 | `registry.build` бросал `KeyError` мимо обработчиков | `_spec()` до сборки адаптера → 422 `kind_unknown`; регрессионный тест | — |
| 17 | CI упал на `ruff` после слияния `ml/multi-query`: `S101` в `eval/offline_e2e.py` | правило bandit включено бэкендом, ветки ML с ним не проверяются | исключение `S101` для `eval/**` в `pyproject.toml` (файлы ML не редактируются); локально перед пушем гонять `ruff check` по всему репозиторию, а не только `src tests` | — |
| 16 | Бюджет запуска считался по просмотренным документам | источник с 200+ неизменными документами никогда не дошёл бы до новых | бюджет — по работе (скачано + упало); тест `test_budget_makes_run_partial_and_never_deletes` | курсор по листингу — не у всех API есть стабильный порядок |
| 18 | `MissingGreenlet` в обратном вызове OAuth при неудачном обмене (повтор класса №12) | `_oauth_failed` читал `connector.tenant_id` после `session.rollback()` | плоские `tenant_id`/`connector_id`/`user_id` снимаются до обмена; в тестах API `account.id` и `tenant_ctx.id` берутся до запроса (сессия теста общая с приложением, commit истекает объекты) | — |
| 19 | Поддельный портал не записывал вызов с истёкшим токеном | проверка авторизации шла до журнала вызовов | журнал — до проверки; тест продления упирался в это, а не в клиент | — |

Не ошибки, но отклонения от контракта ML, принятые ML (handoff v1.2):
режим Р1 — поле компании, а не переменная окружения; `origin` вместо
`answer_source`; `best_fulltext_score` вместо `best_fulltext_rank`.

---

## Этап 0 (25.09, вечер): дизайн коннекторов и исследование

- Дизайн — `DECISIONS.md`, запись «Коннекторы…» с дополнением после
  решений команды. Исследование — `CONNECTORS-RESEARCH.md`.
- Решено командой: права зеркалируются в MVP; Битрикс24 через
  OAuth-приложение; порядок Битрикс24 → Confluence → Яндекс; коннектор =
  система с модулями; тариф от мест, коннекторы не тарифицируются
  (запись в `DECISIONS.md`, «Решения команды»).
- Ключевая находка: REST Битрикс24 не отдаёт права на объекты диска →
  режим «от имени пользователя» (как у Алисы AI), а не выгрузка ACL.
- Ограничение среды: прямые запросы к сайтам Яндекса, Битрикс24,
  Atlassian, Coworker, Habr блокируются прокси; работал через поиск и
  зеркала документации на GitHub. Что не удалось проверить — § 6
  исследования.
- Кода нет; тесты и CI без изменений (798, зелёный).

## Этап 1 (25.09, ночь): ядро коннекторов

**Сделано** (миграция `42a0ae63f5ff`, ~2 900 строк кода и тестов):
- таблицы `connectors`, `connector_user_grants`, `connector_sync_runs`,
  `material_access` под RLS с FORCE; очередь `connector_sync_jobs`;
  у `materials` — `connector_id`, `external_id`, `source_url`,
  `external_version`, `synced_at`, `visibility`; уникальность sha256
  сужена до ручных загрузок;
- `core/secrets.py` (Fernet + ротация, `cli rotate-connector-secrets`),
  `core/outbound.py` (проверка адреса по классам + закреплённый IP +
  ручные редиректы), `connectors/` (интерфейс адаптера, каталог видов,
  очистка HTML);
- `ConnectorSyncService`: режимы `organization` (ACL по почтам) и
  `per_user` (видимость по листингу сотрудника), бюджет, удаление после
  полного листинга, остановка по отвергнутым учётным данным, транзакция
  на документ; `SyncWorker` с планировщиком в том же процессе воркера;
- `/connectors`: каталог, CRUD, учётные данные (только запись),
  проверка, «синхронизировать сейчас», журнал запусков, `mine` для
  сотрудников; лимиты частоты; 9 событий аудита;
- фильтр прав в векторном и полнотекстовом поиске, `source_url` в
  источниках ответа и в материалах; `purge` чистит журнал запусков.

**Проверено:**
- тесты: 143 новых (безопасность 67, синхронизация 20, воркер 12,
  видимость 3, HTML 4, API 41); всего 973 со слиянием ML, покрытие 93 %;
- миграция: `upgrade → check → downgrade → upgrade` на отдельной базе;
  затем `upgrade` на dev-базе `corp_ed`;
- **живая проверка на локальном стенде** (адаптеров реальных систем ещё
  нет — источник поддельный, всё остальное настоящее): компания через
  `cli create-tenant`, API под `uvicorn`, вход и смена пароля,
  `/connectors/kinds` = `[]`, `/connectors/mine` = `[]`, неизвестный вид
  → 422; под ролью `corp_ed_app` (без BYPASSRLS): планировщик поставил
  задачу, `SyncWorker.run_once` выполнил её — docx и pdf через настоящую
  песочницу, HTML-страница через очистку, `.exe` отклонён; 3 материала
  `PENDING` с `source_url`, 3 задачи ингеста, 1 строка `material_access`
  (ACL по почте админа), запуск `partial` со счётчиками, шифротекст в
  `connectors.credentials` без токена, `SELECT count(*) FROM connectors`
  без тенанта = 0; через API: `/connectors`, `/runs`, `/materials` с
  `connector_id` и `source_url`, `/sync` → 202, аудит; процесс
  `python -m corp_ed.worker` стартует и останавливает оба цикла по
  таймауту. Найдена и исправлена ошибка №15.
- не проверено: индексация синхронизированных документов (нет ключей
  Yandex Cloud на этой машине) и настоящий источник (этап 2).

**Дальше** — этап 2, Битрикс24 (сделан ниже).

Доступы к тестовому порталу для живой проверки живут **в переменных
окружения сессии**, не в репозитории и не в чате (договорённость
25.09): `BITRIX24_TEST_PORTAL`, `BITRIX24_TEST_WEBHOOK` (входящий
вебхук с правами `disk`, `landing`, `user`), `BITRIX24_TEST_CLIENT_ID`,
`BITRIX24_TEST_CLIENT_SECRET` (локальное приложение), при необходимости
`BITRIX24_TEST_LOGIN` / `BITRIX24_TEST_PASSWORD` для входа через
безголовый Chromium. Сетевая политика окружения должна разрешать
`*.bitrix24.ru`, `oauth.bitrix24.tech` (сервер авторизации по
документации 2026 года; `oauth.bitrix.info` — прежний адрес),
`apidocs.bitrix24.ru`. Новая сессия начинает с проверки этих
переменных и доступности портала.

## Этап 2 (25.09, ночь): Битрикс24

**Проверка среды в начале сессии.** Переменные на месте:
`BITRIX24_TEST_PORTAL`, `BITRIX24_TEST_WEBHOOK`, `BITRIX24_TEST_CLIENT_ID`,
`BITRIX24_TEST_CLIENT_SECRET` (логин/пароль не заданы, они не
обязательны). Портал, `oauth.bitrix24.tech`/`oauth.bitrix.info` и
`apidocs.bitrix24.ru` **недоступны**: прокси окружения отвечает 403 на
CONNECT (политика организации), в `/__agentproxy/status` —
`connect_rejected` по всем трём хостам. Зеркало документации
`raw.githubusercontent.com/bitrix-tools/b24-rest-docs` доступно —
адаптер написан по нему (страницы методов, OAuth, лимиты, коды ошибок,
скачивание файлов; состояние на 25.09.2026).

**Сделано** (~2 000 строк кода и тестов, без миграций):
- `connectors/bitrix24/`: `client.py` — REST через `OutboundClient`
  (вебхук или `auth` в теле, 2 запроса/с, страницы по `next`, ожидание
  1/2/4 с на `QUERY_LIMIT_EXCEEDED`, коды ошибок → `AdapterAuthError` /
  `AdapterConfigError` / retryable, `expired_token` → продление и один
  повтор, редирект → `portal_moved`, скачивание только с хоста портала
  и с браузерными заголовками из документации, запись ответов без
  токенов); `oauth.py` — адрес авторизации, обмен кода, продление;
  `disk.py` — `disk.storage.getlist` → `getchildren` рекурсивно (общий
  диск и группы; личный диск только свой), корзина/форматы/размер
  отсеиваются в листинге, папка без прав пропускается;
  `knowledge_base.py` — сайты KNOWLEDGE и GROUP → опубликованные
  страницы → HTML активных блоков (`landing.block.getlist` с
  `get_content`); `adapter.py` — спецификация вида (модули `disk`,
  `disk_personal`, `knowledge_base`; config `portal`, `client_id`;
  секрет приложения `client_secret`), `check` через `profile`.
- Ядро: `KindSpec.app_credential_fields` и `user_auth=oauth`;
  `AdapterConfigError` останавливает коннектор, не трогая гранты;
  `refreshed_credentials` сохраняются в грант/подключение при любом
  исходе; `default_registry(settings)`.
- API: `POST /connectors/{id}/oauth/start` (сотрудник), `GET
  /connectors/oauth/callback` (публичная, лимит по IP, редирект на
  `CONNECTOR_OAUTH_RETURN_URL` или JSON), `PUT .../credentials` в
  режиме `per_user` принимает секрет приложения, `PUT .../mine` для
  OAuth-вида — 422 `oauth_required`; каталог отдаёт
  `app_credential_fields`, `oauth`, `oauth_callback_url`, `extra`
  (нужные scope, путь обратного вызова); `/mine` — флаг `oauth`.
  Событие аудита `connector.oauth_failed`. `state` — подписанный JWT
  с `typ=connector_oauth` (`core/security.py`).
- Настройки: `CONNECTOR_OAUTH_CALLBACK_URL`, `CONNECTOR_OAUTH_RETURN_URL`,
  `CONNECTOR_OAUTH_STATE_TTL_MINUTES`, `CONNECTOR_BITRIX24_OAUTH_SERVER`
  (только `https://`).
- `cli connector-check --kind bitrix24 --config portal=… --credential
  webhook=… [--module …] [--fetch N] [--record DIR] [--fast]`: check,
  листинг, скачивание через настоящий конвейер, запись фикстур.
- `OutboundClient.request(allow_redirects=False)`;
  `ingest.extract.SUPPORTED_EXTENSIONS`.

**Проверено:**
- тесты: 93 новых (клиент 32, адаптер и OAuth 32, синхронизация с
  адаптером и базой 5, API OAuth 14, state 4, CLI 5, редирект 1);
  всего 1066, покрытие 92 %; ruff по всему репозиторию, mypy strict —
  чисто;
- контракт: ответы методов из документации (`profile`,
  `disk.storage.getlist`, `disk.folder.getchildren`, `disk.file.get`,
  `landing.site.getlist`, `landing.landing.getlist`,
  `landing.block.getlist`, обмен и продление токена, 16 кодов ошибок)
  лежат в `tests/connectors/fixtures/bitrix24/` и прогоняются через
  адаптер целиком; **это образцы документации, не записи с портала**
  (RISKS №32);
- поддельный портал (`tests/connectors/fake_portal.py`): права на папки
  по пользователю, страницы, истечение и ротация токенов, сервер
  авторизации, лимит запросов, редирект, чужой хост ссылки;
- сквозная синхронизация с настоящим адаптером и базой: материалы с
  `source_url`, `restricted` + `material_access` сотрудника, новая пара
  токенов в гранте после продления, остановка коннектора по
  `invalid_client`/`insufficient_scope` с целыми грантами, истечение
  гранта по `invalid_token` без остановки коннектора.

**Дополнение (та же ночь, автономно): модуль «База знаний 2.0».**
Четвёртый модуль вида `bitrix24` — `knowledge_base_v2`
(`connectors/bitrix24/notes.py`, методы `note.*`, REST 3.0, scope
`note`): `note.collection.list` с курсором → `note.document.tree.list`
→ `note.document.get`, документы приходят **Markdown** и идут в
конвейер как есть (`FetchedMarkdown`, `source_format=md`, те же
границы, что у разбора файла). Дерево не несёт дат, поэтому версия
(`updatedAt`) берётся из `note.document.get` при обходе, а содержимое
запоминается до `fetch`. Клиент научился адресу `/rest/api/` и ошибкам
REST 3.0 объектом `{code, message}` (коды приводятся к нашим:
`insufficient_scope`, `access_denied`, `error_not_found`,
`error_argument`). Адрес документа в интерфейсе REST не отдаёт —
ссылка ведёт в раздел базы знаний портала (RISKS №36). Тесты: 21
новый (клиент REST 3.0, обход, курсор, scope, пропуск закрытого
документа, контракт по образцам `note.*`), плюс синхронизация с базой
и CLI; всего 1088.

**Дополнение: риски 33 и 34 закрыты.** `state` OAuth одноразовый: `jti`
гасится лимитером приложения (`limit=1` на срок жизни state; Redis в
бою), повтор редиректа — `state_reused` без обращения к серверу
авторизации, недоступный Redis — отказ. `OutboundClient.download`
читает файл кусками и обрывает по лимиту — портал не займёт память
воркера гигабайтом; адаптер Битрикс24 переведён на него. Тесты: +5
(одноразовость, обрыв чтения, недоверие `Content-Length`, редирект при
скачивании).

**Дополнение: цепочка поставки (RISKS №18 закрыт).** `uses:` в обоих
workflow переведены на SHA коммитов с комментарием версии (сняты `git
ls-remote --tags`, peeled), `FROM python:3.12-slim` в Dockerfile — на
digest индекса образа из Docker Hub; Dependabot обновляет и то, и
другое. Сервисные образы CI и сканер Trivy остались тегами — почему,
в RISKS №18 (закрыт).

**Живая проверка (26.09, сеть открыта).** Портал, `oauth.bitrix24.tech`,
`oauth.bitrix.info`, `apidocs.bitrix24.ru` доступны. Первый запуск
`connector-check` упал с `network_error`: клиент закреплял IP и слал
CONNECT к IP через egress-прокси среды, а прокси пропускает только по
имени (ошибка №31 → режим `CONNECTOR_OUTBOUND_VIA_PROXY`). Дальше по
вебхуку (права `disk`, `landing`, `user`):
- `profile` ✅; `disk.storage.getlist` ✅ — личный диск админа и «Общий
  диск», формы совпали с фикстурами документации (все значения —
  строки, `total` рядом с `result`, блок `time`); `disk.storage.getchildren`
  ✅ (`DOWNLOAD_URL` с `token`; `DETAIL_URL` — с пробелами, скобками и
  кириллицей без кодирования, RISKS №41); `disk.file.get` ✅; скачаны
  два PDF (49 и 85 страниц), sha256 совпадают между запусками;
- `landing.site.getlist` для `KNOWLEDGE` и `GROUP` — пустые списки: базы
  знаний на тестовом портале нет, модуль не проверен;
- `note.collection.list` — ошибка REST 3.0
  `BITRIX_REST_V3_EXCEPTION_INSUFFICIENTSCOPEEXCEPTION` (у вебхука нет
  scope `note`): разбор кода ошибки 3.0 подтверждён живым ответом, модуль
  не проверен;
- `--record` положил 9 ответов в `tests/connectors/fixtures/bitrix24/live/`
  — без токена и, после ошибки №32, без кода вебхука в пути (проверено
  скриптом по значениям переменных);
- второй PDF песочница убила по лимиту CPU и назвала «повреждённым»
  (ошибка №33): модель разметки `pymupdf-layout` пришла транзитивно с
  `pymupdf4llm` 1.28 и занимает все ядра (23 с по стене = 75 с CPU);
  бюджет теперь таймаут × ядер, убийство по лимиту — `timeout`, решение
  о самой модели — RISKS №40;
- стенд как тест: `tests/live/test_bitrix24_live.py` — компания,
  сотрудник, подключение `bitrix24` `per_user` с модулями
  `disk`/`disk_personal`, грант сотрудника с вебхуком (обход OAuth только
  для стенда), `ConnectorSyncService.run` с настоящей песочницей:
  `SUCCEEDED`, 2 материала `PENDING` с `source_url` портала и извлечённым
  текстом, `material_access` — только сотрудник, второй запуск ничего не
  скачивает; 47 с. Индексация и ответ со ссылкой — нет ключей Yandex
  Cloud в этой среде.

**Не проверено:** OAuth сотрудника с настоящим `oauth.bitrix24.tech`
(нужен вход в портал), база знаний (на портале пустая), «База знаний
2.0» (нет scope `note`), документ в ответе со ссылкой. Этап 2 **не
закрыт** до OAuth и базы знаний; самая рискованная часть — REST,
скачивание, разбор, синхронизация с правами — подтверждена на живом
портале.

**Нужно от команды:**
1. На тестовом портале: база знаний с парой страниц (и одна в группе),
   файлы на «Общем диске»; scope `note` у вебхука, если «База знаний
   2.0» нужна в MVP. Затем `connector-check --record` ещё раз.
2. Для OAuth: `BITRIX24_TEST_LOGIN`/`BITRIX24_TEST_PASSWORD` в переменных
   сессии (безголовый Chromium) или ручной проход; в карточке локального
   приложения «Путь вашего обработчика» → `/api/v1/connectors/oauth/callback`
   стенда (из этой среды — `http://localhost:8000/…`, браузер тоже
   здесь); scope `disk`, `landing`.
3. Решение по модели разметки PDF (RISKS №40).
4. Ротация тестового вебхука: до ошибки №32 его код показался в выводе
   команды этой сессии (журнал сессии; в репозиторий и чат не попал).

## Этап 3 (25.09, ночь, автономно): Confluence Server / Data Center

**Условия.** Документация Atlassian (`developer.atlassian.com`,
`docs.atlassian.com`) из среды недоступна, тестового Confluence нет.
Адаптер написан по устоявшемуся REST API Server/DC (не менялся
годами), формы ответов зафиксированы в поддельном сервере
(`tests/connectors/fake_confluence.py`) — это **наше представление о
Confluence, а не его записи**; список того, что сверять, — RISKS №37.

**Сделано** (`connectors/confluence/`, ~700 строк кода и ~500 тестов):
- `client.py`: PAT (Bearer) или логин/пароль (Basic), `start/limit`
  до пустой страницы или без `_links.next`, 429 с `Retry-After` (до
  30 с, три попытки), 401 → `unauthorized`, 403/404 → пропуск объекта,
  HTML вместо JSON → `not_json` (страница входа SSO), ссылки из
  ответов только на хосте Confluence, скачивание через
  `OutboundClient.download`;
- `adapter.py`: режим `organization`, config `base_url`, `spaces`
  (ключи через запятую, пусто — все глобальные), `email_template`
  (`{username}` → почта); модули `pages`, `attachments`; читатели
  страницы = пересечение ограничений чтения её и предков
  (пользователи + участники групп, кеш на запуск), без ограничений —
  `visibility=tenant`; вложения наследуют читателей, фильтр по
  расширению и размеру; страница с нечитаемым предком пропускается;
- `storage.py`: макросы storage-формата → HTML для общей очистки
  (код, панели, ссылки, задачи, колонки; оглавление/Jira/включения
  выбрасываются).

**Проверено:** 34 теста (клиент, обход, наследование прав, группы,
шаблон почты, фильтр пространств, вложения, storage-формат,
синхронизация с базой: `tenant` против `restricted` с
`material_access` по почте, остановка по отвергнутому токену); всего
1127, покрытие 92 %; ruff и mypy чисто.

**Не проверено:** всё живое. **Нужно от команды:** тестовый
Confluence Server/DC (Docker-образ `atlassian/confluence` с пробной
лицензией или доступ к стенду клиента) и служебная учётная запись с
PAT; затем `cli connector-check --kind confluence --config
base_url=… --credential token=… --record …` и сверка форм ответов.

## Этап 4а (25.09, ночь, автономно): Яндекс Диск

**Условия.** Сайты Яндекса из среды недоступны, организации Яндекс 360
нет. Адаптер написан по устоявшимся REST Диска
(`cloud-api.yandex.net/v1/disk`) и OAuth Яндекс ID (`oauth.yandex.ru`);
формы ответов зафиксированы в поддельных серверах
(`tests/connectors/fake_yandex.py`), список сверки — RISKS №38. Вики
Яндекс 360 не начата: её API без документации не воспроизвести.

**Сделано** (`connectors/yandex/`, ~450 строк кода и ~350 тестов):
- `oauth.py`: адрес авторизации Яндекс ID, обмен кода (POST form),
  продление; новый `refresh_token` сохраняется, если выдан, иначе
  остаётся прежний;
- `disk.py`: клиент (`Authorization: OAuth`, 401 → продление и один
  повтор, 429/503 → ожидание 1/2/4 с, 404/403 папки → пропуск),
  рекурсивный обход `disk:/` с постраничным листингом (`limit/offset`,
  `total`), фильтр по расширению и размеру, версия `md5:modified`,
  ссылка — `public_url` или папка файла в веб-клиенте, скачивание по
  `resources/download` → `href` только на доменах Яндекса, без токена;
- `adapter.py`: вид `yandex360`, режим `per_user` с OAuth, config
  `client_id`, секрет приложения `client_secret`, модуль `disk`; `check`
  через `GET /v1/disk/` (логин сотрудника → `external_user_id`);
- `RemoteDocument.locator` — адрес для fetch отдельно от пути для
  человека (у Диска файл адресуется путём, а не `resource_id`);
- настройки `CONNECTOR_YANDEX_OAUTH_SERVER`, `CONNECTOR_YANDEX_DISK_API`.

**Проверено:** 15 тестов (каталог и сборка, `check`, продление с
сохранением и с ротацией refresh, ожидание при 429, рекурсия и фильтры,
страницы больших папок, скачивание, чужой хост, синхронизация с базой в
режиме `per_user` с сохранением новой пары токенов); всего 1142,
покрытие 92 %; ruff и mypy чисто. Каталог `/connectors/kinds` теперь —
`bitrix24`, `confluence`, `yandex360`.

**Не проверено:** всё живое. **Нужно от команды:** организация
Яндекс 360 с тестовым сотрудником, OAuth-приложение на oauth.yandex.ru
(права `cloud_api:disk.read`, `cloud_api:disk.info`, адрес возврата —
ручка обратного вызова стенда); доступ к `yandex.ru/support` в сетевой
политике — для модуля Вики.

## Самопроверка кода этапов 2–4а (25.09, ночь)

Обзор диапазона `d395f2a..HEAD` встроенным ревью (10 углов, одна
находка воспроизведена на bs4 4.15.0) дал 14 замечаний. Исправлено в
этом коммите:

| # | Замечание | Исправление |
|---|---|---|
| 20 | Обратный вызов OAuth требовал `code`: отказ сотрудника в согласии (`?error=access_denied`, без code) давал 422 JSON в браузере вместо возврата на фронт | `code` необязателен, `error` принимается: аудит `oauth_failed` с кодом `provider_<error>` (или `code_missing`), state гасится, редирект на фронт |
| 21 | `OutboundClient` пересылал `Authorization`/`Cookie` вслед за редиректом на другой хост (PAT Confluence уехал бы на CDN) | заголовки с учётными данными снимаются при смене хоста в `request` и `download`; тесты на оба |
| 22 | `storage_to_html` падал `AttributeError` на макросе, вложенном в удаляемый (`toc` → `info`): не `ExtractionError`, поэтому ронял весь запуск | обход изнутри наружу с пропуском `decomposed`; регрессионный тест |
| 23 | Яндекс: пустой `refresh_token` уходил на сервер, `invalid_request` останавливал коннектор целиком как ошибку приложения | без refresh — `AdapterAuthError('refresh_token_missing')` до сети: гасится один грант |
| 24 | Гонка продления Битрикс24: проверка из API и воркер обменивали один refresh, проигравший гасил грант, хотя новая пара уже в базе | при `invalid_grant` воркер перечитывает шифротекст гранта (`GrantRepository.credentials_of`); изменился — грант жив, повтор следующим запуском; тест с подменой `refresh` |
| 25 | Лимит обратного вызова 30/15 мин на IP: офис за одним NAT упирается при массовом подключении | 300/15 мин (подбор и так ограничен подписью state и лимитом `/oauth/start` по сотруднику) |
| 26 | `_state_reused` без лимитера пропускал (fail open) вопреки замыслу | без лимитера — `state_store_unavailable` |
| 27 | Проверка `https://` для адресов OAuth действовала и в разработке — фронт на `http://localhost` не поднимался | только в `production`; тест на оба режима |
| 28 | `redact` в записи фикстур считал секретом любой ключ `code`: терялись `CODE` сайтов и страниц и `error.code` REST 3.0 | `code` — секрет только в параметрах запроса |
| 29 | Яндекс `fetch` делал лишний запрос метаданных на каждый файл | размер и имя берутся из листинга |
| 30 | Модуль базы знаний 2.0 держал Markdown всей базы в памяти до конца обхода | содержимое отдаётся один раз и удаляется из кеша |
| 31 | `OutboundClient` закреплял IP и слал CONNECT к IP через egress-прокси среды — прокси пропускает только по имени, адаптер не доходил до портала даже при открытой политике (`network_error`) | режим `CONNECTOR_OUTBOUND_VIA_PROXY`: запрос по имени, проверка адреса остаётся (RISKS №39) |
| 32 | `redact` не маскировал код вебхука в пути `DOWNLOAD_URL` (`/rest/{user}/{code}/download/`) — секрет портала ушёл бы в фикстуры | маска сегмента пути; записи сделаны заново и проверены скриптом по значениям переменных |
| 33 | Песочница убивала честный PDF на 85 страниц: 60 с CPU при многопоточной модели разметки (`pymupdf-layout` занимает все ядра: 23 с по стене = 75 с CPU), сотрудник увидел бы «файл повреждён» | бюджет CPU = таймаут × ядер, SIGXCPU/SIGKILL → `timeout` |

Структурные замечания — следующим коммитом: у `KindSpec` появились
`config_check` и `credentials_check` (инварианты вида: `{username}` в
шаблоне почты Confluence, `token` или `username+password`) — 422 при
сохранении формы, а не остановка первой синхронизации; фабрики
адаптеров получают `AdapterOptions` (запись фикстур, без пауз), и
`connector-check --record`/`--fast` действуют для всех видов; общий
`connectors/common.py` (`TokenSet`, `redact`, `json_object`, `to_int`,
`parse_datetime`, `OAUTH_CALLBACK_PATH`) вместо импортов Яндекса из
пакета Битрикс24. Тесты: +2 (инварианты через API, запись фикстур
Confluence и Яндекса).

## Дальнейшие действия — MVP по досье

Что осталось до MVP (3.3): **коннекторы** (10.5). Дизайн — `DECISIONS.md`,
запись «Коннекторы к источникам документов». Форматы «по спросу»
(`.doc`, `pptx`) — предложение, не утверждено; персональные лимиты —
этап расширения.

Правило этапа: тесты → зелёный CI → живая проверка на стенде → запись
здесь. Без живой проверки этап не закрыт.

| Этап | Содержание | Критерий проверки | Что нужно от команды |
|---|---|---|---|
| 0 | Дизайн коннекторов и исследование — **закрыт** | записи в `DECISIONS.md` и `CONNECTORS-RESEARCH.md`, решения приняты | OAuth-приложение Битрикс24 и тестовый портал — к этапу 2 |
| 1 | Ядро: таблицы под RLS, два режима подключения, токены сотрудников и `material_access`, шифрование секретов, `validate_outbound_url`, очередь синхронизации, API `/connectors`, фильтр видимости в поиске — **закрыт** (см. выше) | тесты ядра с поддельным адаптером; SSRF-тесты на все классы адресов; живая проверка на локальном стенде под ролью приложения | `CONNECTOR_SECRETS_KEYS` в `.env` стенда |
| 2 | Битрикс24: OAuth-приложение, диск, база знаний — **диск проверен на живом портале по вебхуку 26.09; OAuth и база знаний ждут стенда** (см. «Этап 2») | контрактные тесты на образцах документации ✅; `cli connector-check` против тестового портала: диск ✅, база знаний и `note` ⏳; синхронизация с настоящей песочницей ✅ (`tests/live/`); OAuth сотрудника ⏳; документ появляется в ответе со ссылкой ⏳ | база знаний и файлы на тестовом портале; логин/пароль для OAuth или ручной проход; решение по модели разметки PDF (RISKS №40) |
| 3 | Confluence Server/DC (режим `organization`, ACL из ограничений и групп) — **код и тесты готовы без документации и живого сервера** (см. «Этап 3», RISKS №37) | контрактные тесты на поддельном сервере ✅; `cli connector-check` против живого Confluence ⏳; документ в ответе со ссылкой ⏳ | доступ к тестовому Confluence или Docker-образ DC с пробной лицензией; доступ к документации Atlassian в сетевой политике |
| 4 | Яндекс Диск и Вики (режим `per_user`) — **Диск: код и тесты готовы без документации и живого Диска** (см. «Этап 4а», RISKS №38); **Вики: не начата**, нужна документация API | контрактные тесты на поддельном сервере ✅; `cli connector-check` против живого Диска ⏳; Вики ⏳ | организация Яндекс 360, OAuth-приложение; доступ к `yandex.ru/support` в сетевой политике |
| 5 | Сквозная проверка MVP на стенде | компания подключена через CLI, три источника, вопросы сотрудников с ссылками на источники, отчёт о пробелах, остановка по кредитам | стенд с `ENVIRONMENT=production`, ключи Yandex Cloud |

Параллельно, вне коннекторов: пороги полнотекста для отчёта о
пробелах и решение по гибриду (ML, по живым логам); ротация ссылок на
actions/образ на SHA/digest (`RISKS.md` №18).
